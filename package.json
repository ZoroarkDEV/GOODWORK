{
  "name": "goodwork-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "GOODWORK - Premium Coworking SaaS Platform (Monorepo)",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "keywords": [
    "coworking",
    "saas",
    "monorepo",
    "react",
    "nextjs",
    "supabase"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.28.0"
  },
  "overrides": {
    "vite": "^8.0.12"
  },
  "dependencies": {
    "react-is": "^19.2.6"
  }
}
